# Task Manager for Library Project (Variant 11)

Навчальний проєкт з дисципліни (Варіант 11): **система управління завданнями у проєкті**, адаптована під домен **бібліотеки**.  
Мета — реалізувати **класи, що реалізують інтерфейси**: `Task`, `Project`, `Notifier`, а також показати роботу з Git (кілька комітів, історія змін, push у віддалений репозиторій).

---

## 🎯 Призначення системи

Система допомагає організувати роботу над бібліотечним ПЗ, дозволяючи:
- створювати та редагувати завдання (наприклад, “Додати модуль книг”, “Реєстрація читачів”, “Оренда/повернення”);
- відстежувати статуси завдань (todo / in_progress / blocked / done);
- об’єднувати завдання в проєкти та керувати їхнім списком;
- розподіляти ресурси (наприклад, години розробника/тестувальника) на завдання;
- надсилати сповіщення про події та зміни в завданнях.

---

## ✅ Вимоги (згідно з умовою)

Проєкт реалізує:
1. **Інтерфейс “Завдання (Task)”** — створення, редагування, зміна/отримання стану завдання.
2. **Інтерфейс “Проєкт (Project)”** — керування списком завдань у проєкті, додавання завдань, розподіл ресурсів.
3. **Інтерфейс “Сповіщення (Notifier)”** — надсилання повідомлень про події та зміни.

---

## 🧩 Функціональність

- `SimpleTask`:
  - створення/редагування завдання;
  - зміна статусу із валідацією;
  - історія змін (лог подій завдання).
- `SimpleProject`:
  - додавання завдань;
  - перегляд списку завдань;
  - призначення ресурсів (allocations) на завдання.
- `ConsoleNotifier` / `MemoryNotifier`:
  - сповіщення в консоль або накопичення в пам’яті (для тестів).

---

## 🗂 Структура проєкту

task_manager/
task_manager/
init.py
interfaces.py # інтерфейси Task, Project, Notifier + ResourceAllocation
models.py # реалізація SimpleTask
project.py # реалізація SimpleProject
notifications.py # ConsoleNotifier, MemoryNotifier
demo.py # приклад використання
tests/
test_flow.py # базові тести
README.md
.gitignore
pyproject.toml

yaml
Копировать код

---

## ⚙️ Вимоги до середовища

- Python **3.10+**
- (опційно) `pytest` для тестів

---

## 🚀 Запуск проєкту

### 1) Клонування репозиторію
```bash
git clone <YOUR_REPO_URL>
cd task_manager
2) Запуск демо

python -m task_manager.demo
🧪 Запуск тестів
Встановити pytest:


python -m pip install -U pytest
Запустити:


pytest -q
🧾 Приклад сценарію (що робить demo)
створює проєкт (наприклад, Library Software Project);

створює 2 завдання (модуль книг, модуль читачів);

змінює статус одного завдання на in_progress;

призначає ресурси (наприклад, Dev/QA години);

виводить завдання та призначені ресурси в консоль.

🌿 Git workflow (приклад)
Приклад логічних комітів у цьому проєкті:

chore: project scaffold and interfaces

feat: implement SimpleTask with status tracking and history

feat: implement SimpleProject with task list and resource allocation

feat: add ConsoleNotifier and MemoryNotifier

test: add demo script and basic tests

Push у GitHub/GitLab:


git branch -M main
git remote add origin <YOUR_REPO_URL>
git push -u origin main
📌 Примітки
Проєкт навчальний і має спрощену бізнес-логіку.
